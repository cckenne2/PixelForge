PixelForge/
├─ README.md
├─ .gitignore
├─ requirements.txt          # PyPI dependencies (torch, torchvision, etc.)
├─ environment.yml           # Conda environment specification
├─ repo_skeleton.txt         
├─ pyproject.toml            # Python packaging config (currently empty)
├─ src/
│  └─ PixelForge/            # Main Python package
│     ├─ __init__.py
│     ├─ data/               # Dataset utilities (loaders, transforms)
│     │  └─ paired.py        # On-the-fly HR/LR paired dataset
│     ├─ models/             # Neural network architectures
│     │  └─ generator.py     # Upscaling generator with AlphaMLP conditioning
│     ├─ train/              # Training utilities and loops
│     │  ├─ train_l1.py      # L1 loss training implementation
│     │  └─ validate.py      # Validation utilities
│     ├─ utils/              # Helper functions
│     │  └─ metrics.py       # Evaluation metrics (PSNR, SSIM, etc.)
│     └─ config/             # YAML configuration files
│        └─ baseline.yaml    # Default training configuration
├─ notebooks/                # Jupyter notebooks for experiments & EDA
│  ├─ 01_data_prep.ipynb     # Data preparation and analysis
│  └─ 02_model_prototype.ipynb # Model prototyping and testing
├─ scripts/                  # CLI entry points and utilities
│  ├─ train.py               # Main training script
│  └─ download_div2k.py      # DIV2K dataset download utility
├─ data/                     # Data directory (gitignored except samples)
│  ├─ raw/                   # Raw datasets (never committed)
│  ├─ processed/             # Preprocessed data (gitignored)
│  ├─ samples/               # Small demo images for testing
│  ├─ HR/                    # High-resolution images
│  └─ LR_bicubic/            # Low-resolution bicubic downscaled images
├─ experiments/              # Training artifacts (gitignored)
│  ├─ logs/                  # Training logs, tensorboard files
│  └─ checkpoints/           # Model weights and checkpoints
├─ tests/                    # Unit tests (not yet implemented)
└─ docs/                     # Documentation (not yet implemented)
